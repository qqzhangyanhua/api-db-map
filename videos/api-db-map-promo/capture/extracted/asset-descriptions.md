# Asset inventory (no-capture)

No website was crawled. Brand look comes from the code-editorial frame preset (tokens.json has no colors/fonts). User-supplied product screenshots:

| File | What it is | Use |
| --- | --- | --- |
| `capture/assets/t03-sequence.jpg` | T·03 接口时序图。`POST /v1/tasks/{task_id}/finalize`，Client → Controller → Service → 五张表，蓝条 SELECT、橙条 UPDATE/INSERT。 | 主证物 |
| `capture/assets/t02-fields.jpg` | T·02 表与字段关系。`recording_task`、`task_grant`、`upload_session`、`upload_part`、`work_job` 卡片与 FK 连线。 | 钉表背景 |
| `capture/assets/index.jpg` | 入口页。接口卡片、路径搜索、读写徽章。 | 收尾闪一下 |

Do not invent additional product screens.
